/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { App } from './src/App';

// Register the root app component
AppRegistry.registerComponent('AegisChat', () => App);

export default App;
